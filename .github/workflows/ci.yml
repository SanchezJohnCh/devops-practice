name: Python CI

on:
  push:
    branches:
      - feature/jobs

jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Building"
     
  
  Test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Testing"

  Deploy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploying"